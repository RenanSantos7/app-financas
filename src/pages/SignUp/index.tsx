import { Text, View } from 'react-native';
import { AppContainer } from '../../styles';

export default function SignUp() {
    return (
        <AppContainer>
            <Text>Cadastro</Text>
        </AppContainer>
    );
};